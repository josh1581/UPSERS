//
//  CallOutController.swift
//  UPSERS
//
//  Created by Joshua Hoyle on 8/18/22.
//

import UIKit
import Firebase

class CallOUtController {
    //MARK: - SOT
   /*
    var callout = CallOut(firstName: "", lastName: "", empID: 0, homeSort: "", callOutDate: NSDate)

    //MARK: - Properties
        
        let db = Firestore.firestore()
        let uid = Auth.auth().currentUser?.uid
        var date = Calendar.current
        
        
        


    //MARK: - CRUD Functions

    func saveCallOutToUserDB(calloutDate: NSDate) {
        let docRef = db.collection("callouts").document(user.employeeID)
        docRef.getDocument { document, error in
            if let document = document, document.exists == false {
                let newCallOut = self.db.collection("callouts").document(user.employeeID)
                newBlockedRef.setData([
                    "firstName" : user.firstName,
                    "lastName" : user.lastName,
                    "empID" : user.empID,
                    "callOutDate" : calloutDate
                    
                ])
            } else {
                return
            }
        }
        
    }
    
    func saveCalloutToCompanyDB() {
        let docRef = db.collection("Corporate").document("Callouts").collection(user.workLocation)
        
    }
 */
}//end of class
