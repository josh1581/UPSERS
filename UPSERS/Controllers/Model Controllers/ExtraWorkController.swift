//
//  ExtraWorkController.swift
//  UPSERS
//
//  Created by Joshua Hoyle on 1/25/23.
//

import Foundation
import Firebase
import FirebaseAuth
import FirebaseFirestore


class ExtraWorkController {
    /*
    //MARK: - SOT
    
    
    var user: User
    var extraWork: ExtraWork
    
    //MARK: - Properties
  
    let db = Firestore.firestore()
    let uid = Auth.auth().currentUser?.uid
    
    
    
    //MARK: - Functions
    
    func saveExtraWorktoUserDB() {
        let extraWorkRef = db.collection("user").document("\(user.employeeID)").collection("extraWork").document()
        extraWorkRef.setData([
            "extraWorkDate" : extraWork.extraWorkDate,
            "firstName" : user.firstName,
            "homeSort" : user.homeSort,
            "lastName" : user.lastName,
            "phoneNumber" : user.phoneNumber,
            "workLocation" : user.workLocation,
            "workRequested" : extraWork.workRequested
        ])
    }
    
    func saveExtraWorkToCorporateDB(){
        let newExtraWorkRef = db.collection("corporate").document(user.workLocation).collection("extraWork").document()
        newExtraWorkRef.setData([
            "extraWorkDate" : extraWork.extraWorkDate,
            
        ])
    }
    
    
    /*
     func saveCallOutToCorporateDB() {
         let newCallOutRef = db.collection("corporate").document(user.workLocation).collection("callOuts").document()
         newCallOutRef.setData([
             "assignedLocation" : user.assignedLocation,
             "callOutDate" : callout.callOutDate,
             "empID" : user.employeeID,
             "firstName" : user.firstName,
             "lastName" : user.lastName,
             "homeSort" : user.homeSort,
         ])
     }
     */
    
    
    
    
*/
    
    
    
}//end of class
